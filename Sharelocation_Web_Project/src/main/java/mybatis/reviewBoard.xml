<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
			"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="reviewBoard.model.ReviewBoardBean">

	<select id="getReview" resultType="MyReview">
		select * from reviewboard
		where spacenum = #{num}
		order by ref desc, restep asc
	</select>

	<select id="getArticleTotalCount" resultType="int">
		select count(*)
		from reviewboard
	</select>
	<select id="getArticleOne" resultType="MyReview">
		select * from reviewboard
		where num = #{num}
	</select>
	<update id="increaseReadcount">
		update reviewboard set readcount = readcount+1 where
		num = #{num}
	</update>
	<!-- 확인 -->
	<insert id="insertArticle">
		insert into
		reviewboard(num,writer,email,subject,passwd,regdate,readcount,ref,restep,relevel,content)
		values(board_seq.nextval,#{writer},#{email},#{subject},#{passwd},sysdate,0,board_seq.currval,0,0,#{content})
	</insert>
	<!-- 확인 -->
	<update id="updateRestep">
		update reviewboard set restep=restep+1 where ref=#{ref} and restep > #{restep}
	</update>
		<insert id="insertReplyArticle">
		insert into reviewboard(num,writer,email,subject,passwd,regdate,readcount,ref,restep,relevel,content,ip)
		values(board_seq.nextval,#{writer},#{email},#{subject},#{passwd},sysdate,0,#{ref},#{restep}+1,#{relevel}+1,#{content},#{ip})
	</insert>








	<select id="getReviewListBySpaceNum" resultType="MyReview">
		select * from reviewboard where spacenum=#{spaceNum}
	</select>
	<select id="getOriginReviewListBySpaceNum" resultType="MyReview">
		select * from reviewboard where spacenum=#{spaceNum} and relevel=0
		order by regdate desc
	</select>
	<select id="getReviewReplyByNum" resultType="MyReview">
		select * from reviewboard where ref=#{num} and relevel = 1
	</select>
	<insert id="insertReply">
		insert into reviewboard(num, spacenum, membernum, write, regdate, content, ref, restep, relevel)
		values(review_board_seq.nextval, #{spacenum}, #{membernum}, #{write}, sysdate, #{content}, #{ref}, #{restep}, #{relevel})
	</insert>
	<delete id="deleteReviewByNum">
		delete reviewboard where num=#{num}
	</delete>
	<select id="getOriginReviewAllCountBySpaceNum" resultType="int">
		select count(*) from reviewboard where spacenum=#{spaceNum} and relevel=0
	</select>
	<select id="getOriginReviewTotalCountBySpaceNum" resultType="int">
		select count(*) from reviewboard where spacenum=#{spaceNum} and relevel=0
	</select>
	<select id="getOriginReviewListByMap" resultType="MyReview">
		select * from reviewboard where spacenum=#{spaceNum} and relevel=0
		order by regdate desc
	</select>
</mapper>
			
			